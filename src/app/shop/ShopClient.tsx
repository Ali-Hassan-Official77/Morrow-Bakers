 "use client";import{useMemo,useState}from"react";import{Search,SlidersHorizontal,X}from"lucide-react";import type{Product}from"@/types";import{ProductCard3D}from"@/components/ProductCard3D";const cats=["All","Breads","Viennoiserie","Cakes","Cookies"];export function ShopClient({products}:{products:Product[]}){const[q,setQ]=useState(""),[cat,setCat]=useState("All");const filtered=useMemo(()=>products.filter(p=>(cat==="All"||p.category===cat)&&(!q||`${p.name} ${p.tagline}`.toLowerCase().includes(q.toLowerCase()))),[products,cat,q]);return <section className="min-h-screen pt-32 pb-20"><div className="container-wide"><div className="max-w-3xl"><p className="text-[10px] font-bold uppercase tracking-[.22em] text-orange">The full counter</p><h1 className="mt-4 font-display text-6xl tracking-[-.05em] sm:text-8xl">Pick your<br/><i>kind of happy.</i></h1><p className="mt-6 max-w-xl text-sm leading-7 text-muted">Everything is baked in small batches. If something sells out, it means somebody got there before you.</p></div><div className="mt-12 flex flex-col gap-3 border-y border-black/10 py-4 md:flex-row md:items-center md:justify-between"><div className="flex gap-1 overflow-x-auto">{cats.map(c=><button key={c} onClick={()=>setCat(c)} className={`whitespace-nowrap rounded-full px-4 py-2 text-[10px] font-bold uppercase tracking-[.12em] ${cat===c?"bg-coffee text-white":"hover:bg-white"}`}>{c}</button>)}</div><label className="flex items-center gap-2 rounded-full border border-black/10 bg-white/65 px-4 py-2.5 md:w-64"><Search size={15} className="text-muted"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search the counter..." className="w-full bg-transparent text-xs outline-none placeholder:text-muted"/>{q&&<button onClick={()=>setQ("")}><X size={14}/></button>}</label></div><div className="mb-5 mt-8 flex items-center justify-between text-[10px] font-bold uppercase tracking-[.16em] text-muted"><span>{filtered.length} bakes</span><span className="hidden sm:flex items-center gap-2"><SlidersHorizontal size={13}/> baked in small batches</span></div>{filtered.length?<div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{filtered.map(p=><ProductCard3D key={p.id} product={p}/>)}</div>:<div className="rounded-[30px] bg-white p-16 text-center"><p className="font-display text-3xl">Nothing on this tray.</p><p className="mt-2 text-sm text-muted">Try another search.</p></div>}</div></section>}