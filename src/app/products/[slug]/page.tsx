import{notFound}from"next/navigation";import Link from"next/link";import{ArrowLeft,Clock3,ShieldCheck}from"lucide-react";import{getProductBySlug,getProducts}from"@/lib/db";import{formatPrice}from"@/lib/format";import{ProductShowcase}from"@/components/ProductShowcase";import{ProductDetailClient}from"./ProductDetailClient";import{ProductCard3D}from"@/components/ProductCard3D";export async function generateStaticParams(){return(await getProducts()).map(p=>({slug:p.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const p=await getProductBySlug((await params).slug);return p?{title:p.name,description:p.tagline}:{}}export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const p=await getProductBySlug((await params).slug);if(!p)notFound();const all=await getProducts();return <section className="pt-32 pb-20"><div className="container-wide"><Link href="/shop" className="mb-8 inline-flex items-center gap-2 text-xs font-bold text-muted hover:text-ink"><ArrowLeft size={15}/> Back to counter</Link><div className="grid gap-10 lg:grid-cols-[1.05fr_.95fr] lg:items-center"><ProductShowcase icon={p.icon} badge={p.badge}/><div className="lg:pl-5"><p className="text-[10px] font-bold uppercase tracking-[.22em] text-orange">{p.category} · {p.weight}</p><h1 className="mt-3 font-display text-6xl leading-[.9] tracking-[-.05em] sm:text-8xl">{p.name}</h1><p className="mt-6 text-base leading-7 text-muted">{p.description}</p><div className="mt-8 flex items-center justify-between border-y border-black/10 py-5"><span className="text-2xl font-bold">{formatPrice(p.price)} <small className="text-xs font-normal text-muted">/ {p.unit}</small></span><span className="flex items-center gap-2 text-[10px] font-bold uppercase tracking-[.12em] text-muted"><Clock3 size={14}/> Fresh batch</span></div><ProductDetailClient product={p}/><div className="mt-6 flex gap-3 text-[10px] text-muted"><span className="flex items-center gap-2"><ShieldCheck size={14}/> Ingredients listed</span><span>•</span><span>Local delivery</span></div></div></div><div className="mt-24"><p className="text-[10px] font-bold uppercase tracking-[.22em] text-orange">More from the counter</p><div className="mt-7 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{all.filter(x=>x.id!==p.id).slice(0,3).map(x=><ProductCard3D key={x.id} product={x}/>)}</div></div></div></section>}